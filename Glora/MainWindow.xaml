<Window x:Name="wind" x:Class="Glora.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Glora"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="MainWindow" Height="auto"  Width="440" WindowStartupLocation="CenterScreen" WindowStyle="None" Background="Black">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Ellipse VerticalAlignment="Center" Grid.Row="0" HorizontalAlignment="Center" x:Name="ellSpeak" Panel.ZIndex="2" Width="15" Height="15" Stroke="White">
            <Ellipse.Fill>
                <RadialGradientBrush>
                    <GradientStop Color="#FFB3B3B3" Offset="0"/>
                    <GradientStop Color="White" Offset="1"/>
                </RadialGradientBrush>
            </Ellipse.Fill>
        </Ellipse>

        <Button x:Name="speakBtn" Click="speakBtn_Click" Grid.Row="0" Height="auto" FontSize="36">
            <Image Source="aaasd.png"></Image>
        </Button>
        <TextBox x:Name="dictateTb" Grid.Row="2" Foreground="White"></TextBox>
        <Border Grid.Row="7">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Button Content="Dictate" x:Name="dictateBtn" Click="dictateBtn_Click" Panel.ZIndex="2" Grid.Column="0" Grid.Row="0" Foreground="White" >
                    
                </Button>
                <Label Grid.Column="1" x:Name="label" Foreground="White"></Label>
            </Grid>
        </Border>
    </Grid>
</Window>
